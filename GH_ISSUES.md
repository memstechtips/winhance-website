# GH_ISSUES (retired)

This file is no longer the source of truth for issue triage state.

- **Triage state** now lives as `triage:*` labels on each GitHub issue (see `memlab` repo `src/lib/mission-control/github-labels.ts`).
- **Auto-close rules** now live in `memlab` repo `src/lib/mission-control/auto-close-rules.ts`.
- **Plan files** continue to live at `.agent-plans/issue-NNN-fix.md` in each project repo.

Retired 2026-05-27 by Memory's Agent on Marco's instruction.
